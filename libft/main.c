#include "libft.h"
#include <string.h>
#include <stdio.h>
int main()
{
    char str [] = "bazz bizz bom bam";
    int c = 'i';    
    // printf("res -> %s" ,ft_strchr(str,c));
    // //printf("res -> %s" ,strchr(str,c));

    //  printf("res -> %s",ft_memchr(str,c,10));
    //printf("res -> %s",memchr(str,c,10));
}